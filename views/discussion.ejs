<!DOCTYPE html>
<html>
  <head>
    <%- include("head"); %>
  </head>
  <body>
  <header>
    <%- include("header"); %>
  </header>
  <main>

    <!-- <div class="screentitle"> -->
      <!-- <form class="newboard" action="/archive" method="POST">
        <button type="submit">Archived boards</button>
      </form> -->

    <!-- <% if(user.utype == "t"){ %>
      <div class="newboardcontainer">
        <form class="newboard" action="/archive" method="POST">
          <button type="submit">Archived boards</button>
        </form>
        <form class="newboard" action="/newdiscussion" method="POST">
          <button type="submit">New board +</button>
        </form>
      </div>
    <% } %> -->

    <div class="pagetitle">
      <h1 class="pagetitle">Discussion boards</h1>
      <div class="pagetitlebut">
        <form class="pagetitlebut" action="/archive" method="POST"><button class="pagetitlebut archive">Archived boards</button></form>
        <form class="pagetitlebut" action="/newdiscussion" method="POST"><button class="pagetitlebut newboard">New board +</button></form>
      </div>
    </div>

    <hr class="pagetitle">

    <div class="screencontent">

      <% activeDiscs.forEach(function(activeDisc) { %>
        <div class="discusspanel">
          <h2 class="discusspanel text"><%=activeDisc.dis_title%></h2>

          <div class="statcontainer">
            <div class="statdiv nonselect">
              <img src="/resources/topic.png" class="staticon">
              <% if(activeDisc.top_count == 1){ %><p class="stattext"><%=activeDisc.top_count%> topic</p><% } else{ %><p class="stattext"><%=activeDisc.top_count%> topics</p><% } %>
            </div>

            <div class="statdiv nonselect">
              <img src="/resources/response.png" class="staticon">
              <% if(activeDisc.res_count == 1){ %><p class="stattext"><%=activeDisc.res_count%> response</p><% } else{ %><p class="stattext"><%=activeDisc.res_count%> responses</p><% } %>
            </div>

            <% if(user.utype == "t" && activeDisc.dis_owner == user.id){ %>
              <form class="statform" action="/archivediscussion" method="POST">
                <button class="statdiv" type="submit">
                  <img src="/resources/archive.png" class="staticon">
                  <p class="stattext">Archive</p>
                </button>
              </form>

              <form class="statform" action="/deletediscussion" method="POST">
                <button class="statdiv" type="submit">
                  <img src="/resources/delete.png" class="staticon">
                  <p class="stattext">Delete</p>
                </button>
              </form>
            <% } %>

          </div>

        </div>
      <% })%>

    </div>

  </main>
  <footer>
    <%- include("footer"); %>
  </footer>
  </body>
</html>